encoder = software
adapter_name = /dev/dri/renderD128
output_name = 0

# Reduce CPU usage: ultrafast uses least CPU (worst quality)
# Options: ultrafast, superfast, veryfast, faster, fast, medium
sw_preset = ultrafast
sw_tune = zerolatency
